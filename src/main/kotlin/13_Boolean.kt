fun main(args: Array<String>) {
//    boolean memiliki nilai true dan false. Nilai 0 dan 1 tidak berlaku untuk menunjukkan nilai pada sebuah variabel bertipe boolean.
    var varBool : Boolean = true
    if(varBool)
        println("varBool bernilai " + varBool)

    /*
    var varInt : Int = 1
    if(varInt)
        println("varInt bernilai " + varInt)

        jika di tambahkan kode program di atas maka akan terjadi error
        karna tipe integer yang di simpulkan pada tipe boolean.
     */
}